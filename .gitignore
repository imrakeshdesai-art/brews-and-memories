node_modules/
dist/
build/
.env
.DS_Store
npm-debug.log*
yarn-error.log*

# Scratch / temporary debug scripts — never commit these
scratch/
*.local.js

# Agent workspace memory — may contain sensitive context
.agents/
